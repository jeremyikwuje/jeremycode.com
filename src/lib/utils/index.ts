/**
 * Shared utility functions used by both the public frontend and admin backoffice.
 *
 * Planned exports (Phase 2+):
 *   slugify(name)       — generate URL-safe slugs (lowercase, hyphens, alphanumeric)
 *   formatDate(iso)     — locale-aware date formatting
 *   truncate(str, n)    — truncate to n chars with ellipsis
 *   wordCount(str)      — word count for TipTap content validation
 */
export {};
